"use strict"; // treat all JS code as the newer version

// alert(3+3); = error // we are using node.js, not browser

console.log(3+3)


let name = "hitesh"
let age = 19
let isLoggedIn = false
let state = null 

// number => 2 to power 53
// BigInt
// string=>
// boolean => true/false
// null => standalone Value
// undefined =>     
// symbol => unique (used to identify)

// object

console.log(typeof undefined) // undefined
console.log(typeof null) // objects